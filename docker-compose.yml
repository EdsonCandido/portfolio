ervices:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: frontend-portfolio

    restart: unless-stopped

    networks:
      - proxy

networks:
  proxy:
    external: true
